% Vertex shader for stereograms.
% Takes coords relative to the window in the range [0,1].
% - passes as range [-1,1] for vertex position.
% - passes as range [0,1] for texture coordinates.
% Author: Ben Challenor

\ () ->
\ () ->
\ [windowCoordX, windowCoordY] ->

  ([windowCoordX * 2 - 1, windowCoordY * 2 - 1, 0, 1], [windowCoordX, windowCoordY])